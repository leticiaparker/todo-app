{
  "name": "todo-app",
  "version": "1.0.0",
  "description": "Todo App com testes Cypress",
  "scripts": {
    "start:backend": "cd backend && node server.js",
    "test:e2e": "npx cypress run",
    "test:e2e:open": "npx cypress open"
  },
  "devDependencies": {
    "cypress": "^13.6.0",
    "serve": "^14.2.0",
    "wait-on": "^7.2.0"
  }
}